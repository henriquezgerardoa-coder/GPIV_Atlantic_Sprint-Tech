appId: com.gpiv.desktop
productName: GPIV Desktop
artifactName: ${productName}-${version}-${os}-${arch}.${ext}
directories:
  output: dist
files:
  - main.js
extraResources:
  - from: ../backend/target/backend-0.0.1-SNAPSHOT.jar
    to: backend/backend-0.0.1-SNAPSHOT.jar
linux:
  icon: assets/icon.png
  target:
    - AppImage
  category: Office
win:
  target:
    - nsis
mac:
  target:
    - dmg

