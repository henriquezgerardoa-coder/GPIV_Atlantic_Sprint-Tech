{
  "name": "gpiv-desktop",
  "version": "0.1.1",
  "private": true,
  "author": "GPIV Team",
  "license": "UNLICENSED",
  "description": "GPIV desktop wrapper (Electron + Spring Boot)",
  "homepage": "https://github.com/gpiv/GPIV_Atlantic_Sprint-Tech",
  "repository": {
    "type": "git",
    "url": "https://github.com/gpiv/GPIV_Atlantic_Sprint-Tech.git"
  },
  "main": "main.js",
  "scripts": {
    "build:backend": "cd .. && mvn -q -pl backend -DskipTests package",
    "version:from-tag": "node scripts/set-version-from-tag.js",
    "start": "electron .",
    "dev": "npm run build:backend && npm run start",
    "smoke": "node --check main.js",
    "dist:dir": "npm run version:from-tag && npm run build:backend && electron-builder --dir",
    "dist:linux": "npm run version:from-tag && npm run build:backend && electron-builder --linux AppImage",
    "dist:win": "npm run version:from-tag && npm run build:backend && electron-builder --win nsis",
    "dist:mac": "npm run version:from-tag && npm run build:backend && electron-builder --mac dmg"
  },
  "build": {
    "extends": "./electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3"
  }
}
